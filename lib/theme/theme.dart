import 'package:flutter/material.dart';

// pallet colors
const negro = Color(0xff335c67);
const naranja = Color(0xffe09f3e);
const amarillo = Color(0xfffff3b0);
const rojo = Color(0xff9e2a2b);
const bordo = Color(0xff540b0e);
const blanco = Color(0xffffffff);

// spacing
const minVerticalSpacing = 12.0;
const minHorizontalSpacing = 8.0;

final themeDefault = ThemeData(
  scaffoldBackgroundColor: blanco,
  useMaterial3: true,
);
